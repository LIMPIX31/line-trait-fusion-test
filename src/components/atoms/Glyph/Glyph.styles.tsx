import styled from 'styled-components'

export const Wrapper = styled.div`
  width: 1rem;
  height: 1rem;
  display: flex;
  justify-content: center;
  align-items: center;
  color: inherit;
  svg {
    width: 100%;
    height: 100%;
    stroke: currentColor;
    fill: currentColor;
  }
`
